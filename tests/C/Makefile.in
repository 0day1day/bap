include Makefile.defs

CFLAGS = -fno-stack-protector

all: $(CEXECS)

$(CEXECS): $(CSRC)
	gcc -m32 $@.c -o $@ $(CFLAGS)
ifeq (@ARCH_X64@, true)
	gcc $@.c -o $@_64 $(CFLAGS)
endif


clean:
	rm -f $(CEXECS)
ifeq (@ARCH_X64@, true)
	rm -f $(CEXECS_64)
endif
